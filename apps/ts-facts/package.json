{
	"name": "ts-facts",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"bin": {
		"ts-facts": "./dist/index.js"
	},
	"scripts": {
		"build": "tsc",
		"compile": "bun run ../../scripts/compile-cli.ts",
		"start": "bun run build && bun dist/index.js",
		"test": "bun test test",
		"lint": "biome check src test",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"ts-facts-core": "workspace:*"
	}
}
